import math
# formula of circle
#radius = float(input("input the radius: "))
#radius = math.pow(radius, 2)

#circumeference = 2 * math.pi * radius
#print(f"the circumeference of circle is {round(circumeference, 2)}cm")

#area = math.pi * radius
#print(f"the area of the circle is {round(area, 2)}cm")

#formula of triangle
a = float(input("input side A: "))
b = float(input("input side B: "))

c = math.sqrt(pow(a,2)+pow(b,2))
print(f"the hypotenuse of triangle is {round(c, 2)}cm")
